import { useState } from "react";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      {/* App component is now just a wrapper - routing is handled in main.jsx */}
    </div>
  );
}

export default App;
